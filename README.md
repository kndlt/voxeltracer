# voxelviewer

## Getting Started

1. Install [Rollup](https://rollupjs.org/), then run following command.
2. Install dependencies by `npm install`
3. Serve with `npm run dev`

Optionally, install [oh-my-zsh](https://github.com/robbyrussell/oh-my-zsh) and be free.
